// DATABASE REMOVED — This seed file is no longer used.
//
// The app now uses a static in-memory session store (src/lib/sessionStore.ts)
// with sample debt data loaded automatically on login. There is no database
// to seed. This file is kept as a stub so it can compile without errors.
//
// If a database is re-introduced in the future, restore the original seed
// logic here and re-add @prisma/client to package.json.

export {};
